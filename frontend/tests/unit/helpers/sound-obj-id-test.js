import { soundObjId } from '../../../helpers/sound-obj-id';
import { module, test } from 'qunit';

module('Unit | Helper | sound obj id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = soundObjId(42);
  assert.ok(result);
});
