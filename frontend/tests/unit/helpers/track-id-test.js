import { trackId } from '../../../helpers/track-id';
import { module, test } from 'qunit';

module('Unit | Helper | track id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trackId(42);
  assert.ok(result);
});
